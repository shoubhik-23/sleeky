import { makeStyles } from "@material-ui/styles";
export const style = makeStyles((theme) => {
  return {
    justifyCenter: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
    },
  };
});
